---
paths:
  - "src/**/*.{ts,tsx,css}"
  - "scripts/**/*.ts"
  - "*.{ts,mjs}"
---

# コードの書き方

データの扱いや外部依存についての制約は `CLAUDE.md`「実装上の約束事」にある。

## コメントの粒度

- コードから読み取れる内容はコメントに書かない（コードの言い換えを禁止）。
- 書くのは「なぜそうするか」= 意図・背景・非自明な制約のみ。
- 1行で足りるならブロックコメントにしない。冗長なら削る。
- 前提が変わった古いコメントは残さず、更新または削除する。
